<%= form_for(@candidatos_turno) do |f| %>
  <% if @candidatos_turno.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@candidatos_turno.errors.count, "error") %> prohibited this candidatos_turno from being saved:</h2>

      <ul>
      <% @candidatos_turno.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :candidato_id %><br>
    <%= f.number_field :candidato_id %>
  </div>
  <div class="field">
    <%= f.label :turno_id %><br>
    <%= f.number_field :turno_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
