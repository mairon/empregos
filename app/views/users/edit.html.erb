<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email, size: 40 %>
  </div>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :cpfcnpj, size: 40 %>
  </div>
  <div class="field">
    <%= f.label :tipo %><br>
    <%= f.radio_button :tipo, 0 %> Administrador
    <%= f.radio_button :tipo, 1 %> Empresa
    <%= f.radio_button :tipo, 2 %> Candidato
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
