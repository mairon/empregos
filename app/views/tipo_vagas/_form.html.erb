<%= form_for(@tipo_vaga) do |f| %>
  <% if @tipo_vaga.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_vaga.errors.count, "error") %> prohibited this tipo_vaga from being saved:</h2>

      <ul>
      <% @tipo_vaga.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
