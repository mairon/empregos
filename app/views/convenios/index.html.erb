<nav>
  <div class="breadCrumb module">
      <ul>
          <li>
              Cadastros
          </li>
          <li>&#187;</li>
          <li>
              <a href="/convenios">Convenios</a>
          </li>
      </ul>
  </div>
</nav>
<div style="color:red;">
<%= flash[:error] %>
</div>
  <div class="search_breadCrumb search">
    <div class="bts_grid">
    </div>
    <div class="row-fluid">
      <div class="span12">
      <%= link_to 'Novo', new_convenio_path, :class => "btn" %>
        <table class="table table-striped table-bordered dTableR" id="dt_c">
          <thead>
            <tr>
              <th></th>
              <th>Nome</th>
            </tr>
          </thead>
          <tbody>
          <% @convenios.each do |convenio| %>
              <td width="15">
                <%= link_to '', convenio, method: :delete, data: { confirm: 'Deseja realmente remover o registro?' }, class: "icon-trash" %>
                <%= link_to '', edit_convenio_path(convenio), class: "icon-pencil" %>
              </td>
              <td width='100'><%= convenio.nome %></td>
            </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</nav>
