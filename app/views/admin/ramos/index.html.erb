<nav>
  <div class="breadCrumb module">
      <ul>
          <li>
              Cadastros
          </li>
          <li>&#187;</li>
          <li>
              <a href="/admin/ramos">Ramos de Atividades</a>
          </li>
      </ul>
  </div>
</nav>
<div style="color:red;">
<%= flash[:error] %>
</div>
  <div class="search_breadCrumb search">
    <div class="bts_grid">
    </div>
    <div class="row-fluid">
      <div class="span12">
      <%= link_to 'Novo', new_admin_ramo_path, :class => "btn" %>
        <table class="table table-striped table-bordered dTableR" id="dt_c">
          <thead>
            <tr>
              <th></th>
              <th>Código</th>
              <th>Nome</th>
            </tr>
          </thead>
          <tbody>
          <% @ramos.each do |state| %>
            <tr>
              
              <td width="15">
                <%= link_to '',[:admin, state], method: :delete, data: { confirm: 'Deseja realmente remover o registro?' }, class: "icon-trash" %>
                <%= link_to '', edit_admin_ramo_path(state), class: "icon-pencil" %>
              </td>
              <td width='100'><%= state.id.to_s.rjust(6,'0') %></td>
              <td width='100'><%= state.nome %></td>
            </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</nav>
