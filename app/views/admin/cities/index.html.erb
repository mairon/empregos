<nav>
  <div class="breadCrumb module">
      <ul>
          <li>
              Cadastros
          </li>
          <li>&#187;</li>
          <li>
              <a href="/cities">Cidades</a>
          </li>
      </ul>
  </div>
</nav>

<div style="color:red;">
<%= flash[:error] %>
</div>
  <div class="search_breadCrumb search">
    <div class="bts_grid">
    </div>
    <div class="row-fluid">
      <div class="span12">
      <%= link_to 'Novo', new_admin_city_path, :class => "btn" %>
        <table class="table table-striped table-bordered dTableR" id="dt_a">
          <thead>
            <tr>
              <th></th>
              <th>Estado</th>
              <th>Nome</th>
            </tr>
          </thead>
          <tbody>
          <% @cities.each do |c| %>
            <tr>
              <td width="165">
              <%= link_to 'Visualizar', admin_city_path(c) %> |
              <%= link_to 'Editar', edit_admin_city_path(c) %> |
              <%= link_to 'Remover', [:admin, c], method: :delete, data: { confirm: 'Deseja realmente remover o registro?' } %></td>
              <td><%= c.state.name %></td>
              <td><%= c.name %></td>
            </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</nav>
