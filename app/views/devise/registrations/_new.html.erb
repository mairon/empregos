<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>
		<div class="cnt_b">
				<div class="input-prepend">
					<span class="add-on">@</span><%= f.email_field :email, placeholder: 'Seu Endereço de email', size: 37 %>	
				</div>			
				<div class="input-prepend">
					<span class="add-on"><i class="icon-user icon-orange"></i></span><%= f.email_field :cpfcnpj, placeholder: 'CPF', class: 'cpf', size: 37 %>
				</div>
				<div class="input-prepend">
					<span class="add-on"><i class="icon-lock icon-orange"></i></span><%= f.password_field :password, autocomplete: "off", placeholder: 'Senha', size: 37 %>
			<% if @validatable %><i>(<%= @minimum_password_length %> characters minimum)</i><% end %>
				</div>
			<div class="input-prepend">
				<span class="add-on"><i class="icon-lock icon-orange"></i></span><%= f.password_field :password_confirmation, autocomplete: "off", placeholder: 'Repita a Senha', size: 37 %>
			</div>
			<div  class="input-prepend">
				<p class="button"><%= f.submit value: 'TERMINEI!' %></p>
			</div>
		</div>	 
<% end %>

