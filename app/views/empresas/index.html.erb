<nav>
  <div class="breadCrumb module">
      <ul>
          <li>
              Cadastros
          </li>
          <li>&#187;</li>
          <li>
              <a href="/empresas">Empresas</a>
          </li>
      </ul>
  </div>
</nav>

<div style="color:red;">
<%= flash[:error] %>
</div>
  <div class="search_breadCrumb search">
    <div class="bts_grid">
    </div>
    <div class="row-fluid">
      <div class="span12">
      <%= link_to 'Novo', new_empresa_path, :class => "btn" %>
        <table class="table table-striped table-bordered dTableR" id="dt_a">
          <thead>
            <tr>
              <th></th>
              <th>Código</th>
              <th>CNPJ</th>
              <th>Nome</th>
            </tr>
          </thead>
          <tbody>
          <% @empresas.each do |empresa| %>
            <tr>
              <td width="40">
                <%= link_to 'Editar', edit_empresa_path(empresa) %> |
                <%= link_to 'Remover', empresa, method: :delete, data: { confirm: 'Deseja realmente remover o registro?' } %>
              </td>
              <td width='100'><%= empresa.id.to_s.rjust(6,'0') %></td>
              <td width='100'><%= empresa.cnpj %></td>
              <td width='100'><%= empresa.razao_social %></td>
            </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
